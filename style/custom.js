$(document).ready(function(){
	/*
	$(".countdown_time").each(function(){
		var tgian = $(this).text();
		$(this).countdown(tgian, function(event) {
			if(event.strftime('%d') != '00'){
				$(this).text(
					event.strftime('%d ngày %H:%M:%S')
				);
			} else {
				$(this).text(
					event.strftime('%H:%M:%S')
				);
			}
		});
	});
	*/
});